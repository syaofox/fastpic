---
description: 开发环境与常用命令（uv、Linux Mint、Docker）
alwaysApply: true
---

# FastPic 开发命令指南

项目使用 **uv** 管理 Python 虚拟环境，开发环境为 **Linux Mint**。Python 3.13+。

## 环境准备

```bash
# 安装 uv（若未安装，Linux Mint 可用）
curl -LsSf https://astral.sh/uv/install.sh | sh

# 进入项目目录后，同步依赖（uv 自动创建 .venv）
uv sync
```

## 日常开发

```bash
# 启动开发服务（默认 0.0.0.0:8000）
uv run uvicorn main:app --host 0.0.0.0 --port 8000

# 热重载（修改代码自动重启）
uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

## 依赖管理

```bash
# 添加新依赖
uv add <包名>

# 添加开发依赖
uv add --dev <包名>

# 更新依赖
uv sync
```

## 手动触发扫描

```bash
curl -X POST http://localhost:8000/scan
```

## Docker 部署

```bash
# 构建镜像
docker compose build

# 启动容器（含 MariaDB）
docker compose up -d

# 停止容器
docker compose down

# 重新构建并启动（代码或依赖变更后）
docker compose build --no-cache && docker compose up -d

# 查看日志
docker compose logs -f
```

## 常用路径

- 图片根目录：`photos/`
- 缩略图缓存：`cache/`
- 静态资源：`static/`
- 数据库：MariaDB（`data/` 目录）

## Docker 环境变量（可选 .env）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `ACCESS_PASSWORD` | `0928` | 访问密码 |
| `MAX_UPLOAD_FILE_SIZE` | `1000M` | 单文件上传上限 |
| `MAX_UPLOAD_TOTAL_SIZE` | `5000M` | 总上传上限 |
| `TZ` | `Asia/Shanghai` | 时区 |
| `PUID` / `PGID` | `1000` | 容器内用户 UID/GID |
