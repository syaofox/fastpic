---
description: 后端代码风格与公共函数复用（FastAPI、SQLModel、路径/搜索工具）
globs: "**/*.py"
alwaysApply: false
---

# 后端代码风格与复用

## 代码风格

- **路由与 Session**：路由与对外 API 用 `async def`，Session 统一 `Depends(get_async_session)`。内部纯计算/IO 用同步 `def`，在路由里用 `await asyncio.to_thread(...)` 调用（参考 utils.folder_tree 中 `get_folder_tree_cached`）。
- **路径规范**：路径参数统一用 `utils.path_utils.normalize_path(path, allow_empty=True)` 或 `allow_empty=False`，非法返回 `None`；需空字符串时用 `normalize_path(...) or ""`。
- **错误与响应**：`raise HTTPException(status_code=4xx/5xx, detail="简短中文描述")`；页面用 `templates.TemplateResponse(...)`，重定向用 `RedirectResponse(url=..., status_code=302)`。
- **命名**：仅模块内部使用的函数以单下划线前缀；可跨模块复用的放在 utils 或 models.py。

## 公共函数与复用

- **SQL LIKE 转义**：做 `ilike`/`like` 前对用户输入用 `utils.path_utils.escape_like(value)`，配合 `LIKE_ESCAPE`（MariaDB 使用 `"!"`，非 `"\\"`），避免 `%`/`_` 被当作通配符。
- **简繁与拼音**：中文搜索可复用 `utils.search` 的 `to_simplified`、`to_traditional`、`to_pinyin_lower`、`search_match`。
- **自然排序**：用 models.py 的 `natural_sort_key(s)`，模型中维护 `filename_natural`/`relative_path_natural` 用于 DB 排序。
- **文件夹树**：`utils.folder_tree` 提供 `get_folder_tree`、`build_nested_tree`、`compute_folder_counts`；带 TTL 缓存用 `get_folder_tree_cached(photos_dir, rel_paths)`；创建/删除文件夹后调用 `invalidate_folder_tree_cache()`。
- **缩略图与扫描**：`utils.images.cache_filename(relative_path)` 供模板与后端共用；新建 Image 记录用 `utils.image_records.create_image_record(...)`；扫描用 `async_session_factory` 分批提交（BATCH_SIZE）。
- **图片查询**：排序列、过滤器复用 `utils.query_builder` 的 `get_sort_column`、`parse_filter_params`、`apply_image_filters`、`apply_image_filters_to_count`。
- **版本号**：从 `config.get_version()` 或 `config.APP_VERSION` 获取，保持单一来源。

## 示例

```python
# 正确：路径规范 + escape_like 再 like
from utils.path_utils import normalize_path, escape_like, LIKE_ESCAPE

path = normalize_path(path, allow_empty=True) or ""
escaped = escape_like(user_input)
stmt = stmt.where(Image.filename.ilike(f"%{escaped}%", escape=LIKE_ESCAPE))

# 错误：直接拼接导致注入或误匹配
stmt = stmt.where(Image.filename.ilike(f"%{user_input}%"))
```

```python
# 正确：同步重计算在路由内用 to_thread
folder_tree = await asyncio.to_thread(get_folder_tree, photos_dir, rel_paths)
```
